   let global = {
       //获取页面高度
       pageHeight: () => {
           return document.body.scrollHeight;
       },
       //获取页面宽度
       pageWidth: () => {
           return document.body.scrollWidth;
       }
   }
   export { global }